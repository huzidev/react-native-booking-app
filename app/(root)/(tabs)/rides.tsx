import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function Rides() {
  return (
    <SafeAreaView>
      <Text>This is Rides history page</Text>
    </SafeAreaView>
  );
}